<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .zoom-controls {
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }
        .zoom-controls label {
            margin-right: 10px;
        }
        .zoom-controls input[type="range"] {
            width: 200px;
            margin: 0 10px;
        }
        .zoom-controls span {
            font-weight: bold;
        }
        iframe {
            border: none;
            width: 100%;
            height: 500px; /* Adjust as needed */
            transform-origin: 0 0;
            margin-top: 10px; /* Space between zoom controls and iframe */
        }
    </style>
</head>
<body>
    <h1>Dashboard</h1>
    <div class="zoom-controls">
        <label for="zoom">Zoom: </label>
        <input type="range" id="zoom" min="0.5" max="3" step="0.1" value="1">
        <span id="zoom-value">100%</span>
    </div>
    <iframe
        id="document-frame"
        src="https://southernwater.sharepoint.com/sites/DirWater/_layouts/15/Doc.aspx?sourcedoc={d796ff40-922a-4b18-aaec-84047bb60fe9}&action=embedview&wdbipreview=True&wdAllowInteractivity=True&wdHideSheetTabs=True&Item=dashboard&wdHideHeaders=True&ActiveCell=A35&AllowTyping=True">
    </iframe>

    <script>
        const zoomSlider = document.getElementById('zoom');
        const zoomValue = document.getElementById('zoom-value');
        const iframe = document.getElementById('document-frame');

        zoomSlider.addEventListener('input', function() {
            const scale = zoomSlider.value;
            zoomValue.textContent = `${Math.round(scale * 100)}%`;
            iframe.style.transform = `scale(${scale})`;
            iframe.style.width = `${100 / scale}%`;  // Adjust width to maintain proportion
        });
    </script>
</body>
</html>
